---
import { format } from "date-fns";
import { fr } from "date-fns/locale";
import MapPanel from "../components/MapPanel.astro";
import {
  clubInfo,
  faqItems,
  getHomeAnnouncements,
  homeHighlights,
  trainingSchedule,
} from "../data/site";
import BaseLayout from "../layouts/BaseLayout.astro";

const proofItems = [
  { num: "01", title: "Cadre", text: "Clair et respecté." },
  { num: "02", title: "Encadrement", text: "Présent et précis." },
  { num: "03", title: "Progression", text: "Réelle et suivie." },
];

const heroBadges = ["Débutants", "Loisir sérieux", "Compétition"];

const heroFacts = [
  { label: "Ville", value: clubInfo.city },
  { label: "Semaine", value: "18h00 - 20h00" },
  { label: "Samedi", value: "10h00 - 11h00" },
];

const homeAnnouncements = getHomeAnnouncements();

const localBusinessSchema = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "SportsClub",
      "@id": `${clubInfo.siteUrl}#club`,
      name: clubInfo.name,
      description:
        "Club de boxe anglaise à La Louvière pour débutants, pratiquants loisir et compétiteurs. Encadrement structuré, cours du soir et progression suivie.",
      url: clubInfo.siteUrl,
      image: `${clubInfo.siteUrl}/hero-boxe.jpg`,
      email: clubInfo.email,
      telephone: clubInfo.phoneHref,
      address: {
        "@type": "PostalAddress",
        streetAddress: clubInfo.addressLine,
        addressLocality: clubInfo.city,
        addressRegion: clubInfo.region,
        postalCode: clubInfo.postalCode,
        addressCountry: clubInfo.countryCode,
      },
      geo: {
        "@type": "GeoCoordinates",
        latitude: clubInfo.latitude,
        longitude: clubInfo.longitude,
      },
      sport: "Boxing",
      hasMap: clubInfo.googleMapsUrl,
      sameAs: clubInfo.socialLinks.map((link) => link.href),
      areaServed: clubInfo.areaServed.map((area) => ({
        "@type": "Place",
        name: area,
      })),
      openingHoursSpecification: trainingSchedule.map((slot) => ({
        "@type": "OpeningHoursSpecification",
        dayOfWeek: `https://schema.org/${slot.schemaDay}`,
        opens: slot.schemaDay === "Saturday" ? "10:00" : "18:00",
        closes: slot.schemaDay === "Saturday" ? "11:00" : "20:00",
      })),
    },
    {
      "@type": "FAQPage",
      "@id": `${clubInfo.siteUrl}#faq`,
      mainEntity: faqItems.map((item) => ({
        "@type": "Question",
        name: item.question,
        acceptedAnswer: {
          "@type": "Answer",
          text: item.answer,
        },
      })),
    },
  ],
};

const schema = JSON.stringify(localBusinessSchema);
---

<BaseLayout
  title={`Club de boxe anglaise à ${clubInfo.city} | ${clubInfo.name}`}
  description={`Club de boxe anglaise à ${clubInfo.city} pour débutants, loisir et compétiteurs. Cours du soir, séance d'essai et encadrement structuré.`}
  image="/hero-boxe.jpg"
  schema={schema}
>
  <section class="hero">
    <div class="hero-decor-line"></div>
    <div class="hero-bg-name">COLAK</div>

    <div class="container">
      <div class="hero-shell">
        <div class="hero-main">
          <div class="hero-badge">
            <span class="line"></span>
            <span>BOXE ANGLAISE • {clubInfo.city.toUpperCase()}</span>
          </div>

          <h1 class="hero-title">
            <span>BOXING CLUB</span><br />
            <span class="text-stroke">COLAK</span>
          </h1>

          <p class="hero-lead">
            Débutants, loisir sérieux et compétition dans un cadre net, suivi et respecté.
          </p>

          <div class="hero-actions">
            <a href="/contact?sujet=essai" class="btn-main">SÉANCE D'ESSAI</a>
            <a href="#annonces" class="btn-secondary-link">VOIR LES ANNONCES</a>
          </div>

          <ul class="hero-tags">
            {heroBadges.map((badge) => <li>{badge}</li>)}
          </ul>
        </div>

        <aside class="hero-side">
          <div class="hero-side-card">
            <span class="hero-side-kicker">Le club</span>
            <p>Un vrai cadre de travail. Pas une pratique approximative.</p>
          </div>

          <div class="hero-facts">
            {heroFacts.map((fact) => (
              <article class="hero-fact">
                <span>{fact.label}</span>
                <strong>{fact.value}</strong>
              </article>
            ))}
          </div>
        </aside>
      </div>

      <div class="proof-points">
        {proofItems.map((item) => (
          <article class="proof reveal">
            <span class="num">{item.num}</span>
            <div class="txt">
              <strong>{item.title}</strong>
              <span>{item.text}</span>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="section-intro">
    <div class="container">
      <div class="intro-grid">
        <div class="intro-title-wrap">
          <span class="section-kicker">Le bon cadre</span>
          <h2 class="title-premium">
            Apprendre, reprendre ou pousser plus loin la boxe anglaise à {clubInfo.city}.
          </h2>
        </div>

        <div class="text-premium">
          <p>Technique, discipline, régularité. Le club s'adresse aux profils qui veulent progresser proprement.</p>
        </div>
      </div>

      <div class="highlights-grid">
        {homeHighlights.map((item) => (
          <article class="highlight-card reveal">
            <h3>{item.title}</h3>
            <p>{item.text}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  {homeAnnouncements.length > 0 && (
    <section class="section-news alt" id="annonces">
      <div class="container">
        <div class="header-flex">
          <h2 class="section-title-small">Annonces du club</h2>
          <div class="line"></div>
        </div>

        <div class="news-track">
          {homeAnnouncements.map((item) => (
            <article class={`news-card ${item.urgent ? "is-urgent" : ""} ${item.image ? "has-image" : ""}`}>
              {item.image && (
                <div class="news-media">
                  <img
                    src={item.image}
                    alt={item.imageAlt ?? item.title}
                    loading="lazy"
                    decoding="async"
                  />
                </div>
              )}

              <div class="news-content">
                <div class="news-meta">
                  <span class="news-category">{item.category}</span>
                  {item.urgent && <span class="badge-urgent">Prioritaire</span>}
                </div>

                <time class="news-date" datetime={item.date}>
                  {format(new Date(item.date), "dd MMM yyyy", { locale: fr })}
                </time>

                <h3>{item.title}</h3>
                <p>{item.excerpt}</p>

                {item.details && (
                  <ul class="news-pills">
                    {item.details.map((detail) => <li>{detail}</li>)}
                  </ul>
                )}

                <a href={item.link.href} class="news-link">{item.link.label}</a>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>
  )}

  <section class="section-schedule" id="horaires">
    <div class="container">
      <div class="schedule-header">
        <div>
          <span class="section-kicker">Organisation</span>
          <h2 class="section-title-center">Planning d'entraînement</h2>
        </div>
        <p class="schedule-copy">Semaine en soirée, plus un créneau femmes le samedi matin.</p>
      </div>

      <div class="schedule-list">
        {trainingSchedule.map((slot) => (
          <div class="schedule-item">
            <div class="item-day">{slot.day}</div>
            <div class="item-content">
              <span class="item-type">{slot.audience}</span>
              <span class="item-time">{slot.time}</span>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <section class="section-faq" id="faq">
    <div class="container">
      <div class="faq-header">
        <span class="section-kicker">Questions fréquentes</span>
        <h2 class="title-premium">Avant de venir</h2>
      </div>

      <div class="faq-list">
        {faqItems.map((item) => (
          <details class="faq-item reveal">
            <summary>{item.question}</summary>
            <p>{item.answer}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <section class="section-contact">
    <div class="container">
      <MapPanel
        title="Un club accessible depuis toute la région du Centre"
        subtitle={`Le ${clubInfo.name} est basé à ${clubInfo.city}. Accès simple, planning clair, contact direct.`}
        detailsTitle="Pratique locale"
      />
    </div>
  </section>

  <script is:inline>
    document.documentElement.classList.add("reveal-ready");

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("is-visible");
        }
      });
    }, { threshold: 0.12 });

    document.querySelectorAll(".reveal").forEach((element) => observer.observe(element));
  </script>

  <style>
    .hero {
      position: relative;
      overflow: hidden;
      padding: 146px 0 78px;
      background:
        radial-gradient(circle at top right, rgba(199, 157, 49, 0.16), transparent 28rem),
        #000;
    }

    .hero-decor-line {
      position: absolute;
      left: 5%;
      top: 0;
      bottom: 0;
      width: 1px;
      background: rgba(199, 157, 49, 0.1);
      z-index: 1;
    }

    .hero-bg-name {
      position: absolute;
      right: -4vw;
      bottom: 2vh;
      font-family: var(--font-accent);
      font-size: 22vw;
      color: rgba(255, 255, 255, 0.03);
      line-height: 0.8;
      z-index: 0;
      pointer-events: none;
    }

    .hero-shell {
      display: grid;
      grid-template-columns: minmax(0, 1.15fr) minmax(280px, 0.85fr);
      gap: 24px;
      padding: 34px;
      border: 1px solid rgba(199, 157, 49, 0.12);
      background:
        linear-gradient(135deg, rgba(199, 157, 49, 0.06), rgba(255, 255, 255, 0.015) 38%),
        rgba(0, 0, 0, 0.78);
      box-shadow: 0 24px 80px rgba(0, 0, 0, 0.22);
      position: relative;
      z-index: 1;
      align-items: start;
    }

    .hero-badge {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 20px;
      color: var(--color-accent);
      font-family: var(--font-accent);
      font-size: 0.92rem;
      letter-spacing: 0.22em;
    }

    .hero-badge .line {
      width: 40px;
      height: 1px;
      background: var(--color-accent);
    }

    .hero-title {
      font-size: clamp(4rem, 10vw, 7.6rem);
      margin-bottom: 18px;
    }

    .hero-lead {
      max-width: 36rem;
      color: #c2c2c2;
      font-size: clamp(1rem, 1.7vw, 1.15rem);
      line-height: 1.7;
    }

    .hero-actions {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      gap: 16px 22px;
      margin-top: 28px;
    }

    .btn-main {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 56px;
      padding: 0 28px;
      background: var(--color-accent);
      color: #050505;
      font-family: var(--font-accent);
      font-size: 1.12rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      box-shadow: 0 18px 40px rgba(199, 157, 49, 0.22);
    }

    .btn-secondary-link {
      font-family: var(--font-accent);
      font-size: 1rem;
      letter-spacing: 0.08em;
      color: #fff;
      border-bottom: 1px solid var(--color-accent);
      padding-bottom: 4px;
      opacity: 0.85;
    }

    .hero-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 22px;
    }

    .hero-tags li {
      padding: 9px 13px;
      border: 1px solid rgba(255, 255, 255, 0.08);
      background: rgba(255, 255, 255, 0.03);
      color: #efefef;
      font-size: 0.9rem;
    }

    .hero-side {
      display: grid;
      gap: 14px;
    }

    .hero-side-card,
    .hero-fact {
      padding: 18px;
      border: 1px solid rgba(255, 255, 255, 0.08);
      background: rgba(255, 255, 255, 0.025);
    }

    .hero-side-kicker,
    .hero-fact span {
      display: inline-flex;
      margin-bottom: 8px;
      color: var(--color-accent);
      font-family: var(--font-accent);
      font-size: 0.82rem;
      letter-spacing: 0.18em;
      text-transform: uppercase;
    }

    .hero-side-card p {
      color: #f0f0f0;
      line-height: 1.55;
    }

    .hero-facts {
      display: grid;
      gap: 12px;
    }

    .hero-fact strong {
      display: block;
      color: #fff;
      font-family: var(--font-accent);
      font-size: 1.45rem;
      letter-spacing: 0.04em;
      line-height: 1.05;
    }

    .proof-points {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 14px;
      margin-top: 16px;
    }

    .proof {
      display: flex;
      gap: 14px;
      padding: 18px;
      border: 1px solid rgba(255, 255, 255, 0.06);
      background: rgba(255, 255, 255, 0.03);
    }

    .num {
      width: 42px;
      height: 42px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      border: 1px solid rgba(199, 157, 49, 0.18);
      color: var(--color-accent);
      font-family: var(--font-accent);
      font-size: 1.15rem;
    }

    .txt strong {
      display: block;
      margin-bottom: 4px;
      color: #fff;
      font-family: var(--font-accent);
      font-size: 1.1rem;
      letter-spacing: 0.05em;
    }

    .txt span {
      color: #9b9b9b;
      font-size: 0.92rem;
      line-height: 1.5;
    }

    .section-intro,
    .section-news,
    .section-schedule,
    .section-faq,
    .section-contact {
      padding: 92px 0;
    }

    .section-news.alt {
      background: rgba(255, 255, 255, 0.015);
    }

    .section-kicker {
      display: inline-flex;
      margin-bottom: 14px;
      color: var(--color-accent);
      font-family: var(--font-accent);
      font-size: 0.9rem;
      letter-spacing: 0.22em;
      text-transform: uppercase;
    }

    .intro-grid,
    .schedule-header {
      display: grid;
      grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
      gap: 34px;
      align-items: start;
    }

    .title-premium,
    .section-title-center {
      font-size: clamp(2.1rem, 4.8vw, 3.2rem);
      color: #fff;
      line-height: 1.05;
    }

    .text-premium,
    .schedule-copy {
      color: #b3b3b3;
      font-size: 1.02rem;
      line-height: 1.68;
    }

    .highlights-grid {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 18px;
      margin-top: 34px;
    }

    .highlight-card {
      padding: 24px;
      border: 1px solid rgba(255, 255, 255, 0.06);
      background: rgba(255, 255, 255, 0.025);
    }

    .highlight-card h3 {
      margin-bottom: 10px;
      color: #fff;
      font-size: 1.3rem;
    }

    .highlight-card p {
      color: #9d9d9d;
      line-height: 1.55;
    }

    .header-flex {
      display: flex;
      align-items: end;
      gap: 20px;
    }

    .section-title-small {
      font-size: 1.15rem;
      color: var(--color-accent);
      letter-spacing: 0.22em;
    }

    .header-flex .line {
      flex: 1;
      height: 1px;
      background: rgba(255, 255, 255, 0.07);
      margin-bottom: 6px;
    }

    .news-track {
      display: grid;
      grid-auto-flow: column;
      grid-auto-columns: minmax(280px, 340px);
      gap: 16px;
      margin-top: 28px;
      overflow-x: auto;
      overscroll-behavior-x: contain;
      scroll-snap-type: x mandatory;
      padding-bottom: 8px;
      scrollbar-width: thin;
      scrollbar-color: rgba(199, 157, 49, 0.5) rgba(255, 255, 255, 0.04);
    }

    .news-track::-webkit-scrollbar {
      height: 8px;
    }

    .news-track::-webkit-scrollbar-thumb {
      background: rgba(199, 157, 49, 0.5);
      border-radius: 999px;
    }

    .news-card {
      display: flex;
      flex-direction: column;
      min-height: 100%;
      border: 1px solid rgba(199, 157, 49, 0.12);
      background: rgba(255, 255, 255, 0.025);
      overflow: hidden;
      scroll-snap-align: start;
    }

    .news-card.is-urgent {
      border-color: rgba(199, 157, 49, 0.28);
      background: linear-gradient(180deg, rgba(199, 157, 49, 0.08), rgba(255, 255, 255, 0.02));
    }

    .news-media {
      padding: 12px 12px 0;
    }

    .news-media img {
      width: 100%;
      height: auto;
      aspect-ratio: 4 / 5;
      object-fit: cover;
      border: 1px solid rgba(255, 255, 255, 0.06);
    }

    .news-content {
      display: flex;
      flex-direction: column;
      min-height: 100%;
      padding: 16px;
    }

    .news-meta {
      display: flex;
      justify-content: space-between;
      gap: 10px;
      margin-bottom: 12px;
    }

    .news-category,
    .badge-urgent {
      display: inline-flex;
      align-items: center;
      min-height: 28px;
      padding: 0 10px;
      font-family: var(--font-accent);
      font-size: 0.8rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    .news-category {
      color: var(--color-accent);
      background: rgba(199, 157, 49, 0.08);
    }

    .badge-urgent {
      background: var(--color-accent);
      color: #050505;
    }

    .news-date {
      margin-bottom: 10px;
      color: #d4d4d4;
      font-size: 0.84rem;
    }

    .news-card h3 {
      margin-bottom: 8px;
      color: #fff;
      font-size: 1.28rem;
      line-height: 1.08;
    }

    .news-card p {
      color: #b8b8b8;
      line-height: 1.55;
      font-size: 0.95rem;
    }

    .news-pills {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-top: 12px;
    }

    .news-pills li {
      padding: 7px 10px;
      border: 1px solid rgba(199, 157, 49, 0.2);
      background: rgba(199, 157, 49, 0.08);
      color: #fff;
      font-size: 0.84rem;
    }

    .news-link {
      margin-top: auto;
      padding-top: 14px;
      color: #fff;
      font-family: var(--font-accent);
      font-size: 0.95rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    .news-link:hover {
      color: var(--color-accent);
    }

    .schedule-header {
      margin-bottom: 28px;
    }

    .schedule-list {
      max-width: 980px;
      display: flex;
      flex-direction: column;
      gap: 14px;
    }

    .schedule-item {
      display: flex;
      align-items: center;
      gap: 22px;
      padding: 22px 24px;
      border-left: 3px solid var(--color-accent);
      background: rgba(255, 255, 255, 0.03);
      border-top: 1px solid rgba(255, 255, 255, 0.04);
      border-right: 1px solid rgba(255, 255, 255, 0.04);
      border-bottom: 1px solid rgba(255, 255, 255, 0.04);
    }

    .item-day {
      width: 120px;
      flex-shrink: 0;
      color: var(--color-accent);
      font-family: var(--font-accent);
      font-size: 1.75rem;
      letter-spacing: 0.04em;
    }

    .item-content {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .item-type {
      color: #fff;
      font-size: 1rem;
      font-weight: 700;
    }

    .item-time {
      color: #9a9a9a;
      font-size: 0.96rem;
    }

    .faq-header {
      display: flex;
      flex-direction: column;
      align-items: start;
      gap: 6px;
      margin-bottom: 24px;
    }

    .faq-list {
      display: grid;
      gap: 14px;
    }

    .faq-item {
      border: 1px solid rgba(255, 255, 255, 0.06);
      background: rgba(255, 255, 255, 0.025);
    }

    .faq-item summary {
      list-style: none;
      cursor: pointer;
      padding: 18px 20px;
      color: #fff;
      font-family: var(--font-accent);
      font-size: 1.2rem;
      letter-spacing: 0.04em;
    }

    .faq-item summary::-webkit-details-marker {
      display: none;
    }

    .faq-item p {
      padding: 0 20px 18px;
      color: #a9a9a9;
      line-height: 1.55;
    }

    .faq-item[open] {
      border-color: rgba(199, 157, 49, 0.18);
    }

    .reveal {
      opacity: 1;
      transform: none;
    }

    :global(html.reveal-ready) .reveal {
      opacity: 0;
      transform: translateY(30px);
      transition: 0.9s cubic-bezier(0.16, 1, 0.3, 1);
    }

    :global(html.reveal-ready) .reveal.is-visible {
      opacity: 1;
      transform: translateY(0);
    }

    @media (max-width: 1080px) {
      .hero-shell,
      .intro-grid,
      .schedule-header {
        grid-template-columns: 1fr;
      }

      .highlights-grid {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

    @media (max-width: 820px) {
      .hero {
        padding-top: 122px;
      }

      .hero-shell {
        padding: 24px;
      }

      .hero-bg-name,
      .hero-decor-line {
        display: none;
      }

      .proof-points,
      .highlights-grid {
        grid-template-columns: 1fr;
      }

      .header-flex {
        flex-direction: column;
        align-items: start;
        gap: 12px;
      }

      .header-flex .line {
        width: 100%;
      }

      .news-track {
        grid-auto-columns: minmax(270px, 320px);
      }
    }

    @media (max-width: 640px) {
      .hero {
        padding: 116px 0 68px;
      }

      .hero-title {
        font-size: 3.7rem;
      }

      .hero-actions {
        flex-direction: column;
        align-items: stretch;
      }

      .btn-main {
        width: 100%;
      }

      .hero-side-card,
      .hero-fact,
      .proof,
      .highlight-card {
        padding: 18px;
      }

      .section-intro,
      .section-news,
      .section-schedule,
      .section-faq,
      .section-contact {
        padding: 74px 0;
      }

      .news-track {
        grid-auto-columns: 86%;
      }

      .news-pills {
        flex-direction: column;
      }

      .schedule-item {
        flex-direction: column;
        align-items: start;
      }

      .item-day {
        width: auto;
      }

      .faq-item summary {
        font-size: 1.05rem;
      }
    }
  </style>
</BaseLayout>
